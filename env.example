# JustiFi Configuration (Payment Processing)
# For stdio mode (local development), these are required:
JUSTIFI_CLIENT_ID=your_justifi_client_id_here
JUSTIFI_CLIENT_SECRET=your_justifi_client_secret_here

# For HTTP mode with OAuth, these are optional (bearer tokens used instead)
# Client credentials only needed for stdio mode or dev testing

# Base URL Configuration (choose one based on your environment)
JUSTIFI_BASE_URL=https://api.justifi.ai                    # Production (default)
# JUSTIFI_BASE_URL=https://custom-api.example.com          # Custom environment
# JUSTIFI_BASE_URL=http://localhost:3000                   # Local development

# OAuth 2.1 Configuration (for hosted/remote deployments)
# Used when MCP_TRANSPORT=http
OAUTH_ISSUER=https://justifi.us.auth0.com                  # OAuth authorization server
OAUTH_AUDIENCE=https://api.justifi.ai                      # API audience identifier
OAUTH_SCOPES=openid,profile,email                          # Comma-separated OAuth scopes

# Optional: Tool Selection (comma-separated list)
# Default: No tools enabled (secure by default)
# JUSTIFI_ENABLED_TOOLS=all                                    # Enable all tools
# JUSTIFI_ENABLED_TOOLS="retrieve_payout,list_payouts"         # Enable specific tools only
# JUSTIFI_ENABLED_TOOLS="retrieve_payout,get_payout_status"    # Custom combination

MCP_TRANSPORT=http
MCP_HOST=0.0.0.0
#MCP_PORT=3000

# Optional: Logging Configuration
# LOG_LEVEL=INFO                    # DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)

# Note: AI model API keys (OpenAI, Anthropic, etc.) are NOT needed for the MCP server.
# Those are handled by MCP clients (Claude Desktop, Cursor, etc.) that connect to this server.


 